import { PlayerAuthCallback } from "@/components/player-auth-callback";

export const metadata = {
  title: "Confirm Player Account | Normie"
};

export default function PlayerAuthCallbackPage() {
  return <PlayerAuthCallback />;
}
