import { PlayerPasswordResetScreen } from "@/components/player-password-reset-screen";

export const metadata = {
  title: "Reset Player Password | Normie"
};

export default function PlayerPasswordResetPage() {
  return <PlayerPasswordResetScreen />;
}
